"""Phase 2 stub: read Telemetry JSON lines from ESP32 USB/UART serial."""
from __future__ import annotations

# pip install pyserial  (Phase 2 only)
# Example:
#   import serial
#   from protocol import SERIAL_BAUD, parse_line
#   ser = serial.Serial("/dev/ttyUSB0", SERIAL_BAUD, timeout=0.05)
#   line = ser.readline().decode(errors="ignore")
#   telem = parse_line(line)
# Swap stdin in gauge_ui.py for this once taps are live.
